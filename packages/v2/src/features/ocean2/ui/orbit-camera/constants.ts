export const ORBIT_TARGET: [number, number, number] = [0, 0, 0];
export const ORBIT_MIN_DISTANCE = 1;
export const ORBIT_MAX_DISTANCE = 100;
/** Keep the camera above the sea floor, which is a single-sided plane. */
export const ORBIT_MAX_POLAR_ANGLE = Math.PI / 2 - 0.05;
