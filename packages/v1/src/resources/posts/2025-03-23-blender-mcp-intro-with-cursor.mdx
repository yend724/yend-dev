---
title: 【BlenderMCP】CursorでBlenderを操作する
date: 2025-03-23T19:30:00
description: BlenderMCPを使ってCursorからBlenderを操作する環境の導入手順を解説。macOS環境での設定方法。
tags:
  - Blender
  - Cursor
draft: false
---

[BlenderMCP](https://github.com/ahujasid/blender-mcp)を用いて、CursorでBlenderを操作する導入まで。

## 前提

- macOS Sonoma 14.6.1
- [Cursor](https://www.cursor.com/ja) 0.45.14
- [Blender](https://www.blender.org/) 4.4.0
- [uv](https://docs.astral.sh/uv/) 0.6.9

## uvのインストール

BlenderMCPはPythonで実装されている。Pythonのパッケージマネージャーとして `uv` を使用する。

未インストールの場合は、以下のコマンドでインストールすること。

```shell
$ brew install uv
$ uv --version
uv 0.6.9 (Homebrew 2025-03-20)
```

## Blenderの設定

### Blender Add-onのインストール

1. [BlenderMCPのGitHubリポジトリ](https://github.com/ahujasid/blender-mcp)から `addon.py` をダウンロードする。
2. Blenderを起動する。
3. メニューから `Edit > Preferences > Add-ons` を選択する。
4. 「Install from Disk...」をクリックし、ダウンロードした `addon.py` を選択する。

![Install from Diskの選択](/assets/posts/2025-03-23-blender-mcp-intro-with-cursor/blender-mcp-addon-install-01.png)

`addon.py`をインストールすると、以下のように表示される。

![Add-onのインストール後](/assets/posts/2025-03-23-blender-mcp-intro-with-cursor/blender-mcp-addon-install-02.png)

### Blender Add-onの起動

インストールが完了したら、Blender Add-onを起動する。

1. Blenderを開く。
2. サイドバーを表示する（右上の `<` ボタン、または `N` キー）。
3. サイドバーの `BlenderMCP` タブを開き、「Start MCP Server」ボタンをクリックする。

「Start MCP Server」が「Stop MCP Server」に変わっていれば、MCPサーバーが正常に起動している。

![Start MCP Serverの選択](/assets/posts/2025-03-23-blender-mcp-intro-with-cursor/blender-mcp-addon-start.png)

## Cursorの設定

最後にCursorの設定を行う。

1. `Cursor > Preferences > Cursor Settings > Features > MCP Server` からCursorにMCPサーバーを設定する。
2. 「Add new MCP Server」をクリックする。
3. 設定内容は以下の通りである：
   - **Name**：任意の名前（例：`BlenderMCP`）
   - **Type**：`command`
   - **Command**：`uvx blender-mcp`
4. 左側のインジケーターが緑色になっていれば、設定は完了である。

![MCP Serverの設定](/assets/posts/2025-03-23-blender-mcp-intro-with-cursor/blender-mcp-cursor-settings.png)

## CursorでBlenderを操作する

設定が完了すると、MCPを通じてCursorからBlenderを操作できるようになる。ただし、現時点ではCursorからMCPサーバーを呼び出すためには、[Agent](https://docs.cursor.com/chat/agent)機能を使用する必要がある。

以下は、実際にCursorを使ってBlenderを操作している様子。

<EmbedTweet id="1903658114496553152" />

## おわりに

[MCP](https://modelcontextprotocol.io/introduction)すごい。

## 参考

- [BlenderMCP - Blender Model Context Protocol Integration](https://github.com/ahujasid/blender-mcp)
- [Setup tutorial: Using a Blender MCP with Cursor AI (on Mac)](https://www.youtube.com/watch?v=wgWsJshecac)
